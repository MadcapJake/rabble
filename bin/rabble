#!/usr/bin/env perl6
use v6.c;
use lib 'lib';
use Rabble;

multi sub MAIN(Str $expr, Bool :expression(:$e), Bool :debug(:$d)) {
  my $*DEBUG = $d.so;
  my Rabble $rabble .= new;
  $rabble.run($expr)
}
